export interface UserEntry {
    id: number
    email: string
    role: string
}

export interface RoleEntry {
    id: number
    name: string
}

export interface CityEntry {
    id: number
    name: string
}
